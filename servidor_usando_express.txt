
Crie um diretorio
        mkdir servidor
        cd servidor

Rode npm init
        npm init -y

Instale o modulo express
npm install express        

digite o programa

inicie com
        nome servidor.js


// para git com usuario diferente do global
git config --local user.name "sergiolmm-pucc"
git config --local user.email "sergio.marques@puc-campinas.edu.br"

git config --local credential.helper ""
git push origin master        